# Images

Drop real photos here before publishing:

- `portrait.jpg` — hero photo (displayed at 260x320, object-fit: cover)
- Hobby photos — filename referenced from each hobby's `image` field in `src/data/hobbies.ts` (displayed at 112x112, circular crop)

Until a file is added, that slot falls back to an "Add photo" placeholder automatically.
